
// NOTE object below must be a valid JSON
window.heart = $.extend(true, window.heart, {
    "config": {
        "layoutSet": "split",
        "navigation": [
            {
                "title": "About",
                "action": "#About",
                "root": false
            }
        ]
    }
});
